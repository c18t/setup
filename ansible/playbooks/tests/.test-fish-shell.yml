---

- hosts: local
  roles:
  - fish-shell
  vars: # 指定できる変数は各ロールの defaults/main.yml 参照
    # fish-shell
    fish_shell_path:
  vars_prompt:
    - name: my_password
      prompt: "User password"
      private: yes
